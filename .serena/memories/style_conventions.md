Style/conventions:
- No explicit formatter/linter config found in pyproject; follow existing project patterns.
- Python code uses snake_case, logging via `logging` module, and type hints appear in some modules.
- Prefer minimal, consistent changes that match surrounding style.